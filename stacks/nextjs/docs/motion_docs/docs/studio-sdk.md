# Studio SDK Overview

> Source: https://motion.dev/docs/studio-sdk

The Motion Studio SDK allows developers to build with the same functions and components as used by [Motion Studio](./studio).

The SDK provides React components, state, and code generation APIs. Development is ongoing, but in this early version currently provides:

  * [Easing curve visualisation](./studio-sdk-easing-curve)

  * [Code generation](./studio-sdk-codegen) for CSS and Motion transitions

  * Functions for [serialising editor state](./studio-sdk-state) into and out of the URL

## Install

Add the `motion-studio` package to your project using your [private token](https://plus.motion.dev). You need to be a [Motion+ member](../plus) to generate a private token.

npm install "https://api.motion.dev/registry.tgz?package=motion-studio&version=4.1.1&token=YOUR_AUTH_TOKEN"

## License

[Motion+](../plus) allows you to build **internal animation tooling** with Motion Studio SDK. To build externally-facing tooling for your customers, [contact us](https://x.com/motiondotdev) about the Motion+ Builder's License.
